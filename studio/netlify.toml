[dev]
  command = "sanity start"
  port = 8888
  targetPort = 3333
  framework = "#custom"
  publish = "dist"

[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
